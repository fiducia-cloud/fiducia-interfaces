# .github — repository automation

GitHub-hosted configuration for this repo:

- `dependabot.yml` — weekly dependency-update PRs for the npm toolchain and GitHub Actions.
- `workflows/` — CI that regenerates and validates the interface artifacts.
